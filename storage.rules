rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read and write for development
      // TODO: Add proper authentication rules for production
      allow read, write: if true;
    }
  }
}
